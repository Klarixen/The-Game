//Final score
//

const FinalScore = () => {
   // const [showFinalResult, set FinalResult] = useState(true);
    
    return (
        //{showFinalResult ? (
        <div className="score">
            <h1>Your Final Score</h1>
            <h2>
                1 out of 15 correct
            </h2>
        </div>
      //  ) : (
//use more questions
       
   // )}
   
    );
}
    
export default FinalScore;